#include<iostream>
using namespace std ;

int main(){



//-----------------
///find nonrepetd elemnt
    // int a[]={3,1,4,2,3,2,1};
    // int arrsize=sizeof(a)/sizeof(a[0]);
    
    // for(int i=0;i<arrsize;i++)
    // cout<<a[i]<<" ";
    // cout<<endl ;

    // // int unique=0;
    // for(int i=0;i<=arrsize;i++)
    // {
    //     for(int j=i+1;j<=arrsize;j++){          
    //           if(a[i] == a[j])
    //           { a[i]=a[j]=(-1);}
    //     }
    // }
    // for(int i=0;i<arrsize;i++)
    // {
    //     if(a[i]>0)
    //     {cout<<a[i]<<endl ;}
    // }
    // // cout<<"\nUnique No: "<<unique<<endl ;
//-----------------------  




//-----------------
///no of pairs==sum threeloop
    // int a[]={3,1,2,4,0,6};
    // int arrsize=sizeof(a)/sizeof(a[0]);
    
    // for(int i=0;i<arrsize;i++)
    // cout<<a[i]<<" ";
    // cout<<endl ;

    // int x,triplet=0;
    // cout<<"Enter x: ";cin>>x;

    // for(int i=0;i<arrsize;i++)
    // {
    //     for(int j=i+1;j<arrsize;j++){
    //        for(int k=j+1;k<arrsize;k++){
    //           if((a[i]+a[j]+a[k])==x)
    //           { triplet++;}
    //         }
    //     }
    // }
    // cout<<"No of Triplet: "<<triplet<<endl ;;
//-----------------------  


///here u can do dry run also
//to avoid confusion


//-----------------
///no of pairs==sum two loop
    // int a[]={3,4,6,7,1};
    // int arrsize=sizeof(a)/sizeof(a[0]);
    
    // for(int i=0;i<arrsize;i++)
    // cout<<a[i]<<" ";
    // cout<<endl ;

    // int x,pair=0;
    // cout<<"Enter x: ";cin>>x;

    // for(int i=0;i<arrsize;i++)
    // {
    //     for(int j=i+1;j<arrsize;j++)
    //     if((a[i]+a[j])==x)
    //     {pair++;}
    // //   else  //else yha work nhi karra..kuch alag sochna padega
    // //   {cout<<"No pair is present";}
    // }
    // cout<<"Pair: "<<pair<<endl ;;
//-----------------------    
    return 0;
}